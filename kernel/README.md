# Kernel Implementation Placeholder

This directory is reserved for future executable AICL kernel and compiler implementation artifacts.

The formal kernel specification currently lives in:

* [`../docs/kernel/`](../docs/kernel/)

The detailed semantic support documents live in:

* [`../docs/kernel/semantic/`](../docs/kernel/semantic/)

Do not place formal specification documents directly in this root-level `kernel/` directory.

Expected future contents may include:

* reference parser
* verifier
* SHG builder
* proof obligation generator
* ROM generator
* materializer runtime adapters
