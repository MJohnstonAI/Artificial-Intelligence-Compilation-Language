# WKG Domains

This directory is reserved for domain-specific WKG extensions built on the core substrate.

Expected contents over time include:

- domain ontologies
- sector-specific capability vocabularies
- policy-domain extensions
- example grounding material

Domain material here should remain compatible with `wkg/core/` and should not bypass the canonical spec layer.
