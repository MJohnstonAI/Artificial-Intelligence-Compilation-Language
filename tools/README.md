# Tools

This directory is reserved for repository tooling related to AICL.

Expected contents over time include:

- validators
- parser sketches
- schema checks
- draft transformation utilities

Tooling here should support the canonical documents under `spec/` rather than become an alternate source of language semantics.
